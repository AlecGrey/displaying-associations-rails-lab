<h1><%= @artist.name %></h1>

<h2><%= @artist.verbose_song_count %>:</h2>

<ul>
    <% @artist.songs.each do |song| %>
        <li><%= link_to song.title, song_path(song) %></li>
    <% end %>
</ul>

<%= link_to "Edit Artist", edit_artist_path(@artist) %>
<br>
<%= link_to "Go Back", artists_path %>